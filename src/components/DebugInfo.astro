---
// デバッグ情報表示コンポーネント
---

<div class="fixed bottom-4 right-4 bg-black/80 text-white p-4 rounded-lg text-xs font-mono max-w-md">
  <h4 class="font-bold mb-2">Debug Info</h4>
  <div>BASE_URL: {import.meta.env.BASE_URL}</div>
  <div>PROD: {import.meta.env.PROD}</div>
  <div>MODE: {import.meta.env.MODE}</div>
  <div>SITE: {import.meta.env.SITE}</div>
</div>